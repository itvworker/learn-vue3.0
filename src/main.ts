import '../vue3.0/packages/vue/dist/vue.global.js';

// TypeScript error? Run VSCode command
// TypeScript: Select TypeScript version - > Use Workspace Version
import App from './App.vue'

window.Vue.createApp(App).mount('#app')
